'use client';
import { Flex, Heading } from '@chakra-ui/react';

export const Advertising = () => (
  <Flex
    bg={'gray'}
    minH={'250px'}
    flexDirection={'row'}
    alignItems={'center'}
    justifyContent={'center'}
    h={'full'}
  >
    <Heading as="h3">Publicidade</Heading>
  </Flex>
);
